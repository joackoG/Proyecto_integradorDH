<section id="nav">
    <nav>

        <div id="log">
            <a href="/"><img src="/img/Logo.png" width="100" alt="logo"></a>
            <h1>Shenlong Comics</h1>
        </div>
        
        <div class="navBar">
            <!-- <ul type="none"> -->
                 <%= console.log(locals.isLogged)   %>
                <% if (locals.isLogged) { %>
                    <div class="container-link">
                        <form action="/users/logout" method="POST" class="nav-link">
                            <button type="submit" class="btn-link">Cerrar sesión</button>
                        </form>
                    </div> 
                    <div>
                        <a href="/users/profile" class="btn-link">Ver Perfil</a>
                    </div>
                <% } else { %>
                    <a href="/users/register">Registrarse</a>
                    <a href="/users/login">Iniciar sesión</a>
                    
                <% } %>
                <div class="icon-card">
                    <a href="/productCart" title="Ver carrito de compras"><i class="fa-solid fa-cart-shopping"></i></a>

                </div>



                       
    </nav>
</section>