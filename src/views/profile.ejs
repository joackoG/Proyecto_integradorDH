<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('./partials/head', { estilo: 'profileStyle', titulo: 'Perfil de usuario' }) %>

<body>
    <!-- Header -->
    <%- include('partials/headNavBar', { title: 'nav' , usuario: usuario }) %>

    <!-- /Header -->

    <div class="container-perfil">
        <div class="card-perfil">
            <!-- <div class=""> -->
                <h1 class="">Página de perfil</h1>
                <div class="container-img">
                        <img class="img-usuario"
                            src="/img/imgUsuario/<%= usuario.fotoPerfil %>"
                            alt="Foto de perfil de usuario">
                 
                </div>

                <div class="info">
                    <h3>El usuario que inició sesión es: <%= usuario.nombre %></h3>
                    <h3>Su correo electrónico es: <%= usuario.correo %></h3>
                    <h3>Su Fecha de nacimiento es : <%= new Date(usuario.fechaNac).toLocaleDateString() %></h3>
                </div>

                <div class="container-btn">
                    <a class="submit-btn" href="/users/editUser/<%= usuario.id %>">Editar usuario</a>
                </div>

                <div class="container-link">
                    <form action="/users/logout" method="POST" class="nav-link">
                        <button type="submit" class="btn-link">Cerrar sesión</button>
                    </form>
                <!-- </div> -->
            </div>
        </div>
    </div>

    <!-- footer -->
    <%- include('./partials/footer') %>
    <!-- /footer -->
</body>

</html>
